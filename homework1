<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        //Задать температуру в градусах по Цельсию. Вывести в alert соответствующую температуру в градусах по Фаренгейту. 
        //Подсказка: расчёт идёт по формуле: Tf = (9 / 5) * Tc + 32, где Tf – температура по Фаренгейту, Tc – температура по Цельсию
        var temperature = prompt("Введите температуру");
        alert("По Фаренгейту это " + (9 / 5 * temperature + 32));
    </script>
    <script>
        //Объявить две переменные: admin и name. Записать в name строку "Василий"; 
        //Скопировать значение из name в admin. Вывести admin (должно вывестись «Василий»).
        var admin;
        var name = "Василий";
        admin = name;
        alert(admin)
    </script>
    <script>
        //Чему будет равно JS-выражение 1000 + "108"
        //Будет выведено 1000108, потому что в выражении присутствует строка, а оператор "+" в таком случае отработает 
        //не как арифметическое действие в виде сложения, а как конкатенация элеменитов в выражении 
    </script>
    <script>
        //Самостоятельно разобраться с атрибутами тега script (async и defer).
        //Скрипты с async выполняются в том порядке, в котором они успели загрузиться. То есть, если скрипт маленький и легкий, 
        //он выполнится первым, вне зависимости от того, где он расположен в коде.
        //Скрипты с defer выполняются в порядке очереди. Если первым в коде идет большой и тяжелый скрипт, то все остальные скрипты 
        //будут ждать, пока загрузится и выполнится этот скрипт и только потом начнет выполняться следующий по очередли скрипт
    </script>
    <script>
        //Поменять значения в переменных не используя третью переменную
        var a = 10;
        var b = 20;

        a = a + b;
        b = a - b;
        a = a - b;
        console.log("а стало равно " + a + " в стало равно " + b);
    </script>
</head>

<body></body>

</html>
